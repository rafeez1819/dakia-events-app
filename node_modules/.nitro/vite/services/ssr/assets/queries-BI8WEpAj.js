import { t as createServerFn } from "../index.js";
import { t as createServerRpc } from "./createServerRpc-A6pJPYTF.js";
import { t as buildSnapshotFromEvents } from "./engine-CfoH9ADi.js";
import { i as loadRecentEvents, r as loadEventsSince, t as countByOrigin } from "./store-e_Pha8XU.js";
//#region src/lib/analytics/queries.ts?tss-serverfn-split
var getIntelSnapshot_createServerFn_handler = createServerRpc({
	id: "6965dde659a9df9761e56f679ed55ad254108c59bed38275fa2b9720d6fd9031",
	name: "getIntelSnapshot",
	filename: "src/lib/analytics/queries.ts"
}, (opts) => getIntelSnapshot.__executeServer(opts));
var getIntelSnapshot = createServerFn({ method: "GET" }).validator((input) => ({ rangeDays: [
	7,
	30,
	90
].includes(Number(input?.rangeDays)) ? Number(input.rangeDays) : 30 })).handler(getIntelSnapshot_createServerFn_handler, async ({ data }) => {
	const rangeDays = data.rangeDays;
	const now = Date.now();
	const since = now - rangeDays * 864e5;
	const prevSince = now - rangeDays * 2 * 864e5;
	const [events, prevWindow, recent, realtimeEvents, origin] = await Promise.all([
		loadEventsSince(since),
		loadEventsSince(prevSince),
		loadRecentEvents(80),
		loadEventsSince(now - 18e5),
		countByOrigin()
	]);
	const prevEvents = prevWindow.filter((e) => e.timestamp < since);
	return buildSnapshotFromEvents({
		events,
		prevEvents,
		recent,
		realtimeEvents,
		rangeDays,
		seededEvents: origin.seed,
		liveEventsCount: origin.live
	});
});
//#endregion
export { getIntelSnapshot_createServerFn_handler };
